diff --git a/src/ol/css.js b/src/ol/css.js
index 88a6d5c9d..f6a10ac5c 100644
--- a/src/ol/css.js
+++ b/src/ol/css.js
@@ -17,6 +17,7 @@
 /**
  * The CSS class for hidden feature.
  *
+ * @api
  * @const
  * @type {string}
  */
@@ -26,6 +27,7 @@ export const CLASS_HIDDEN = 'ol-hidden';
 /**
  * The CSS class that we'll give the DOM elements to have them selectable.
  *
+ * @api
  * @const
  * @type {string}
  */
@@ -35,6 +37,7 @@ export const CLASS_SELECTABLE = 'ol-selectable';
 /**
  * The CSS class that we'll give the DOM elements to have them unselectable.
  *
+ * @api
  * @const
  * @type {string}
  */
@@ -44,6 +47,7 @@ export const CLASS_UNSELECTABLE = 'ol-unselectable';
 /**
  * The CSS class for unsupported feature.
  *
+ * @api
  * @const
  * @type {string}
  */
@@ -53,6 +57,7 @@ export const CLASS_UNSUPPORTED = 'ol-unsupported';
 /**
  * The CSS class for controls.
  *
+ * @api
  * @const
  * @type {string}
  */
@@ -63,6 +68,7 @@ export const CLASS_CONTROL = 'ol-control';
  * The CSS class that we'll give the DOM elements that are collapsed, i.e.
  * to those elements which usually can be expanded.
  *
+ * @api
  * @const
  * @type {string}
  */
@@ -93,6 +99,8 @@ const fontRegExMatchIndex = [
 /**
  * Get the list of font families from a font spec.  Note that this doesn't work
  * for font families that have commas in them.
+ *
+ * @api
  * @param {string} fontSpec The CSS font property.
  * @return {FontParameters} The font parameters (or null if the input spec is invalid).
  */
